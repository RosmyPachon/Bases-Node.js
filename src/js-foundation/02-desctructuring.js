// console.log(process.env);

const { SHELL, HOMBREW_PREFIX, npm_lifecycle_script } = process.env;

// console.table({ SHELL, HOMBREW_PREFIX, npm_lifecycle_script });


const characters = ['flash', 'superman', 'batman'];

const [ , , , batman] = characters; 

// console.log(batman)